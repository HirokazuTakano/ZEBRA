＜承認待ち＞

string sql =
               "SELECT * " +
                 "FROM dbo.TM_DAILY_REPORT " +
                "WHERE  APPROVAL_STATUS = 1 AND AUTHOR_ID = @EMP_ID";

// パラメタに値を設定
            command.Parameters.Add(
                    new SqlParameter("@EMP_ID", MainMenu.loginUser.EmpId));


＜NG＞

string sql =
               "SELECT * " +
                 "FROM dbo.TM_DAILY_REPORT " +
                "WHERE  APPROVAL_STATUS = 2 AND AUTHOR_ID = @EMP_ID";